{
  "mcpServers": {
    "osrlib": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}/server", "osrlib-referee-mcp"],
      "env": {
        "OSRLIB_REFEREE_ADVENTURES_DIR": "${CLAUDE_PLUGIN_ROOT}/adventures"
      }
    }
  }
}
